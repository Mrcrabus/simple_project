import React from 'react';
import styles from './AboutHeader.module.css'

const AboutHeader = () => {
    return (
        <div className={styles.header__about}>
            <div className={styles.hero__about}>We love writing</div>

        </div>
    );
};

export default AboutHeader;
